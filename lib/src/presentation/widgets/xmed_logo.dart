import 'package:flutter/material.dart';

class XmedLogo extends StatelessWidget {
  const XmedLogo({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Image.asset('assets/images/logo.png'));
  }
}
